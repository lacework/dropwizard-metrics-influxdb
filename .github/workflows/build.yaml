name: Build & Push to Artifactory

on:
#   pull_request:
#     branches: 
#       - "main"

  push:
    branches:
      - "artifactory"

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
    - name: Checkout repository
      uses: actions/checkout@v4
